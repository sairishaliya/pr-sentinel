# Real Run Proof

This file exists to trigger a real pull request workflow run for PR-Sentinel.

The expected result is a GitHub Actions run that posts a PR-Sentinel review comment using the offline mock provider.
